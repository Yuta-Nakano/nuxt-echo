export const state = () => ({})

export const mutations = {}

export const actions = {
  // async useheaderSocket({ commit }, { id, key, value }) {
  //   const queries = new URLSearchParams()
  //   queries.append(key, value)
  //   const { data } = await this.$axios.put('api-url/' + id, queries, {
  //     headers: {
  //       'X-Socket-ID': this.$echo.socketId()
  //     }
  //   })
  //   commit('mutationName', data)
  // },
  // listenEcho() {
  //   this.$echo
  //     .channel('laravel_database_channel-name')
  //     .listen('PublicEvent', (e: any) => {
  //       console.log(e)
  //     })
  // },
}
